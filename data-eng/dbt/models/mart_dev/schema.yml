# mart_dev models: analytics-ready; write to mart_dev (BQ_DATASET_GOLD).
# Run: dbt run --target mart_dev

version: 2

models:
  - name: gold_example
    description: "Example mart model. Replace with aggregations and ML-ready logic."
    columns:
      - name: id
        description: "Primary key"
