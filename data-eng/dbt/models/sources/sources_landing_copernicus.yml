version: 2

sources:
  - name: landing_copernicus
    database: opentrace-prod-5ga4
    schema: 'landing_copernicus'
    description: 'BigQuery dataset `landing_copernicus`'
    tables:
      - name: _placeholder
        description: No tables in catalog; run bq_schema_catalog.py then re-run this script.

