# Notebooks (non-production)

Use for ad hoc exploration. Production logic belongs in **`dbt/`**, **`airflow/`**, **`data/sql/`**, or **`ml/`** as appropriate.

Pipeline design notebooks remain in **`../data-pipelines/`**; you can copy experiments here under `exploration/` or `archive/` if you want them separated from staged ETL design.
