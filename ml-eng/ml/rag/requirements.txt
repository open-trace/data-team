# RAG graph and retrieval (install from repo root: pip install -r ml-eng/ml/rag/requirements.txt)
langgraph>=0.2.0
langchain-core>=0.3.0

# API (for ml.rag.api / Hugging Face deploy)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Llama via Hugging Face Inference API / reranker + generator
requests>=2.31.0
PyYAML>=6.0

# Qdrant + local/HF embeddings (sentence-transformers needs torch).
# Pin NumPy 1.x: torch wheels are often built against NumPy 1.x (NumPy 2 breaks torch import).
numpy>=1.24.0,<2.0
# torchvision: transformers (pulled by sentence-transformers) lazy-imports vision modules;
# Streamlit's file watcher can trigger those imports and crash without torchvision.
torch>=2.4.0
torchvision>=0.17.0
qdrant-client>=1.9.0
fastembed>=0.4.0
# transformers 5.x requires torch>=2.4 (unavailable on some Intel macOS builds — max 2.2.2).
transformers>=4.44,<5
huggingface_hub[hf_xetra]
sentence-transformers>=3.0,<5
tiktoken>=0.5.0
pypdf>=4.1.0
llama-index-core>=0.11.0
pydantic>=2.0
# Optional Unstructured (numba/llvmlite): pip install -r ml-eng/ml/rag/requirements-preprocess-optional.txt
unstructured>=0.10.0

# Streamlit (for local RAG testing UI)
streamlit>=1.28.0

# Already in repo requirements.txt; listed here for standalone install
google-cloud-bigquery>=3.14.0
